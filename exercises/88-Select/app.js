var arr = ['a', 'c', 'e'];
var obj = {
  a: 1,
  b: 2,
  c: 3,
  d: 4
};

select = (a, b) => {

    for (i = 0; i < a.length; i++) {  

        if (a == b[i]){

            newObject = ;
        }
    };    
select (arr, obj);